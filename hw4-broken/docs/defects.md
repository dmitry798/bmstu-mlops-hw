# Разбор дефектов hw4-broken

Вход стадии — датасет из ДЗ 3 (`iamtarun/python_code_instructions_18k_alpaca`,
переработанный: 5 вариантов системного промпта, ответы проверены `ast.parse`,
групповой сплит по теме). `data/train.jsonl` — 1739 диалогов, `data/val.jsonl` — 218.
Модель — `Qwen/Qwen3-0.6B`, `enable_thinking: false`. `tests/check.sh` не менялся,
отпечаток `f2002c8c9519` совпадает с выданным.

## Дефект 1. Промпт попадал в лосс

**В чём был.** В `src/tokenize_data.py` функция `mask_prompt` возвращала
`list(input_ids)` — labels равнялись input_ids целиком, а граница промпта в
`encode_example` была захардкожена `n_prompt = 0`. Готовая `prompt_token_len`
из `src/prompt.py` не вызывалась вовсе.

**Как проявлялся.** Модель штрафовали за каждый токен системного промпта и
вопроса, поэтому она училась воспроизводить формулировку вопроса. Отчёт стадии
писал «в лосс идут почти все токены»; проверка 1 падала — под маской
декодировался весь диалог, а не `messages[-1].content`.

**Как исправлен.** `encode_example` строит строку промпта через
`build_chat_text(..., add_generation_prompt=True)`, токенизирует полный текст
с `return_offsets_mapping=True` и получает границу через `prompt_token_len`
(основной путь — совпадение префикса токенов, запасной — символьные офсеты
при склейке BPE). `mask_prompt` ставит `-100` на первые `n_prompt` позиций.

**Чем подтверждается.** До исправления в лосс шло 100% токенов train.
После: 192 726 из 350 857 (54,9%), промпт — 45%. Проверка 1 сверила
1739 примеров из 1739: под `label != -100` только ответ ассистента.
Запасной путь по офсетам сработал на 0 примерах (шаблон заканчивает промпт `\n`),
проверка 6 на синтетике «Приве» / «Привет, мир» даёт границу на токене 3.

## Дефект 2. Шаблоны обучения и инференса расходились

**В чём был.** В `src/prompt.py` путь обучения (`add_generation_prompt=False`)
собирал текст руками по строке `TURN = "<|im_start|>{role}\n{content}<|im_end|>\n"`,
а путь инференса — через `tokenizer.apply_chat_template`. Шаблон Qwen3 при
`enable_thinking=false` вставляет в начало ответа ассистента пустой блок
`<think>\n\n</think>\n\n`, ручная сборка его не знала.

**Как проявлялся.** Модель училась отвечать сразу после `assistant\n`, а на
инференсе видела перед ответом `<think></think>` — формат, которого в обучении
не было. Метрики обучения такое не показывают. Проверка 2 и проверка 3
(спецтокены ролей строкой в `src/`) были красными.

**Как исправлен.** Константа `TURN` удалена; оба пути идут через один вызов
`apply_chat_template` с тем же `enable_thinking`, отличается только
`add_generation_prompt`. Других вызовов шаблона в проекте нет.

**Чем подтверждается.** Со старой ручной сборкой строка инференса не была
префиксом строки обучения в 218 диалогах val из 218; расхождение на байте 246:
инференс `assistant\n<think>\n\n</think>\n\n`, обучение `assistant\nclass Vector3D:`.
После исправления проверка 2 — 218 из 218 совпадают побайтово, проверка 3 зелёная.

## Дефект 3. Паддинг справа

**В чём был.** Два места. `DynamicPaddingCollator` по умолчанию имел
`padding_side="right"`; в `main()` параметр `tokenize.padding_side` из
`params.yaml` не применялся к токенизатору (TODO), а у токенизатора Qwen3 по
умолчанию `padding_side = "right"` — это значение и уходило в `train.pt` и метрики.

**Как проявлялся.** В decoder-only модели при батче > 1 у коротких примеров
между концом промпта и первым сгенерированным токеном оказывались pad-токены
(`pad_token_id = 151643`) — мусор в начале ответа. При batch = 1 паддинга
нет, поэтому «у меня всё работает».

**Как исправлен.** Дефолт коллатора — `"left"`; в `main()` добавлено
`tokenizer.padding_side = params["tokenize"]["padding_side"]`, так что
значение из `params.yaml` одинаково попадает в токенизатор, `train.pt` и
`metrics/tokenize.json`.

**Чем подтверждается.** До: `padding_side = "right"` в метриках и `train.pt`.
После: `left` везде; проверка 4 собирает батч из двух примеров разной длины —
`attention_mask` начинается с 0 и заканчивается 1, labels на паддинге `-100`.

## Дефект 4. Обрезка по max_seq_len не считалась (чинится в два приёма)

**В чём был.** `truncation_stats` возвращала `{}` — отчёт писал «обрезка НЕ
ПОСЧИТАНА», в метриках не было `truncated` / `truncated_ratio`. Вторая часть:
`max_seq_len: 232` в `params.yaml` не соответствовал распределению длин.

**Как проявлялся.** Обрезка с конца съедает ответ, а не промпт: часть
датасета тихо терялась. Проверка 5 падала — метрики нет.

**Как исправлен.** Шаг 1: `truncation_stats` считает число и долю примеров
с `full_len > max_seq_len` по всем записям и печатает предупреждение, если
доля выше `tokenize.truncated_warn_ratio`. После этого при 232 стадия
показала: train обрезано 452 из 1739 (26,0%), val 58 из 218 (26,6%), 9
примеров потеряли ответ целиком и выброшены — выше порога 5%.
Шаг 2: `max_seq_len` выбран по хвосту распределения. Длины train в токенах:
p50 178, p90 321, p95 398, p97 571, p99 1470, max 4041. Доля обрезанных
(train / val): 384 — 5,5% / 7,8%; 512 — 3,1% / 4,6%; 640 — 2,7% / 2,3%;
1024 — 1,6% / 0,9%. Выбран 512 (≈p96): первое «круглое» значение ниже порога
на обоих сплитах. Дальнейший рост почти не снижает долю обрезанных,
но из-за квадратичного внимания заметно дороже; хвост >1000 токенов — единичные
очень длинные программы.

**Чем подтверждается.** Проверка 5: train 3,1%, val 4,6% при пороге 5,0%;
примеров без ответа после обрезки — 0 (было 9). Всего токенов train 350 857,
прогноз обучения 3 эпохи при 52 ток/с — 20 242 с (5,6 ч), реально в 2–3 раза
дольше, т. к. 52 ток/с — скорость генерации. Packing: 1739 → 701 бин,
шагов при batch_size 8: 218 → 88 (−60%).
